name: pcan
description: PCAN CAN bus driver bindings for Dart (FFI).
version: 2.0.1
repository: https://github.com/Kitjesen/brainstem

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  ffi: ^2.1.4
  code_assets: ^1.0.0
  hooks: ^1.0.0

dev_dependencies:
  ffigen: ^20.0.0
  lints: ^6.0.0
  test: ^1.25.6
