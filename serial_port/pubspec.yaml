name: serial_port
description: Serial port driver bindings for Dart (FFI + CMake).
version: 1.1.5
repository: https://github.com/Kitjesen/brainstem

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  ffi: ^2.1.4
  code_assets: ^1.0.0
  hooks: ^1.0.0
  native_toolchain_cmake: ^0.2.0
  logging: ^1.3.0

dev_dependencies:
  ffigen: ^20.1.0
  lints: ^6.0.0
  test: ^1.25.6
