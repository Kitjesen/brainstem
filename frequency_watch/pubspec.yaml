name: frequency_watch
description: Frequency measurement utility for hardware control loops.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
