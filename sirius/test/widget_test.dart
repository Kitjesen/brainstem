import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app smoke test placeholder', (WidgetTester tester) async {
    // Placeholder: desktop app requires window_manager initialization
    // which cannot be run in unit test context.
    expect(true, isTrue);
  });
}
